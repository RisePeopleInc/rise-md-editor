node_modules
out
release
dist
.DS_Store
*.log
.vite
.cache
.env
.env.local

# Claude Code local state — per-machine working directories, scheduled
# task locks, etc. If we ever start sharing settings (slash commands,
# subagents) via .claude/settings.json or similar, those entries can be
# tracked explicitly with `!.claude/settings.json`-style negations.
.claude/

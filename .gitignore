node_modules
out
release
dist
.DS_Store
*.log
.vite
.cache
.env
.env.local

# Intermediate iconset PNGs generated by scripts/generate-file-icons.sh
# on the way to build/file-icons/md.icns. The committed binaries are the
# .icns / .ico; the per-size PNGs are disposable build artifacts.
build/file-icons/md.iconset/

# Claude Code local state — per-machine working directories, scheduled
# task locks, etc. If we ever start sharing settings (slash commands,
# subagents) via .claude/settings.json or similar, those entries can be
# tracked explicitly with `!.claude/settings.json`-style negations.
.claude/
